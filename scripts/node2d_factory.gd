class_name Node2DFactory

static func spawn_pickup_effect(pos: Vector2, text: String) -> void:
	# Ví dụ: Instantiate một floating_text.tscn tại vị trí pos
	# var text_fx = preload("res://scenes/effects/floating_text.tscn").instantiate()
	# text_fx.global_position = pos
	# text_fx.set_text(text)
	# get_tree().current_scene.add_child(text_fx)
	pass
